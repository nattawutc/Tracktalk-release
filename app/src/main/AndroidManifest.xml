<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.blackpuppydev.tracktalk_release">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />

    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tracktalkrelease">
        <activity
            android:name=".app.LoginPageActivity"
            android:exported="false" />
        <activity
            android:name=".app.SettingActivity"
            android:exported="false" />
        <activity
            android:name=".app.BaseActivity"
            android:exported="false" />
        <activity
            android:name=".app.SplashScreenActivity"
            android:exported="false" />
        <activity
            android:name=".demo.TestActivity"
            android:exported="false" />
        <activity
            android:name=".app.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".service.Recognizer" />
    </application>

</manifest>